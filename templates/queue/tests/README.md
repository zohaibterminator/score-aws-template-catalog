# Queue checks

`scripts/validate.sh` initializes this directory without a backend and validates the pinned SQS queue and DLQ module. Queue output names and types are checked by the contract validator.
