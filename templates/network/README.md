# Network

Creates four subnet tiers through the pinned VPC module, with optional NAT, or returns caller supplied existing network IDs. Existing subnets and routing are not managed. Client routing from an external Kubernetes cluster must be provided separately.
