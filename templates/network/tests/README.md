# Network checks

`scripts/validate.sh` initializes this directory without a backend and validates the pinned VPC module. New- and existing-VPC input cases are exercised by `scripts/test-catalog.py`.
