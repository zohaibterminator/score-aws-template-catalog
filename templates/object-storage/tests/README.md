# Object storage checks

`scripts/validate.sh` initializes this directory without a backend and validates the pinned S3 module. Public-access and production force-destroy safeguards are checked by `scripts/test-catalog.py`.
