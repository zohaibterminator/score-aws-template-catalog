# Cache

Creates private ElastiCache Redis or Valkey with encrypted storage and transit, restricted ingress, and a dedicated subnet group. An optional auth token is accepted as a sensitive input. Production uses a two-node replication group and snapshot retention.
