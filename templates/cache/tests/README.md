# Cache checks

`scripts/validate.sh` initializes this directory without a backend and validates pinned ElastiCache and security-group modules. Production token input, private ingress, and output types are covered by root contract tests.
